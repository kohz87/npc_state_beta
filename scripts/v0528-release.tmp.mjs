import fs from 'node:fs';

const read = p => fs.readFileSync(p, 'utf8');
const write = (p, s) => fs.writeFileSync(p, s);
const replaceOnce = (text, before, after, label) => {
  const count = text.split(before).length - 1;
  if (count !== 1) throw new Error(`${label}: expected 1 match, found ${count}`);
  return text.replace(before, after);
};
const replaceRegexOnce = (text, re, after, label) => {
  const matches = text.match(re);
  if (!matches || matches.length !== 1) throw new Error(`${label}: expected 1 regex match, found ${matches?.length || 0}`);
  return text.replace(re, after);
};

// Prompt authority: the visible anchor remains the visible wording; World_State may only
// enrich that already grounded individual with one compatible canonical name.
{
  const p = 'src/evidence-adapter.js';
  let s = read(p);
  s = replaceOnce(s,
    "'NEW identity needing contextual binding uses identityEvidence:{anchor,excerpts,explanation}; anchor is an actual current-visible proper name or unique role, excerpts are 1-3 exact current-visible quotes, and missing name/title parts are never invented.',",
    "'NEW identity needing contextual binding uses identityEvidence:{anchor,excerpts,explanation}; anchor is the actual current-visible proper/short name or unique role/description that identifies the individual, excerpts are 1-3 exact current-visible quotes, and missing name/title parts are never invented.',",
    'identity prompt');
  s = replaceOnce(s,
    "'A visible unique short-name anchor may be enriched by one compatible canonical full name in the same current World_State; without the visible anchor, structured/reference blocks cannot manufacture identity.',",
    "'A unique current-visible identity anchor (proper/short name or unique role/description) may be linked to one compatible canonical full name in the same current World_State. Keep identityEvidence.anchor as the visible wording; a structured-only canonical name belongs in name, never anchor. Without grounded visible identity evidence, structured/reference blocks cannot introduce the NPC.',",
    'structured identity prompt');
  write(p, s);
}

// One identity admission authority. Exact current-visible identity evidence is validated first;
// canonical-name enrichment is then allowed only from current World_State placement. The older
// English role-head heuristic is removed rather than retained as a competing decision engine.
{
  const p = 'src/scan-application.js';
  let s = read(p);
  s = replaceOnce(s,
    "import { evidenceReferenceScope } from './evidence-adapter.js';",
    "import { } from './evidence-adapter.js';",
    'evidence adapter import placeholder');
  // Remove the now-unused empty import after all replacements.
  s = s.replace("import { } from './evidence-adapter.js';\n", '');

  s = replaceRegexOnce(s,
    /function restrictedEvidenceScope\(state, patch, policy\) \{[\s\S]*?\n\}\nfunction referenceAllowedForActivity/,
    'function referenceAllowedForActivity',
    'restrictedEvidenceScope');

  const oldIdentity = `function identityEvidenceVerified(patch, policy, currentAdmissionText = '') {\n    const record = identityEvidenceRecord(patch);\n    if (!record) return null;\n    const anchor = humanIdentityCandidate(record.anchor, patch?.role);\n    const explanation = String(record.explanation || '').trim();\n    const canonicalName = canonicalPatchName(patch, []);\n    const visible = currentVisibleEvidenceText(policy, currentAdmissionText);\n    if (!anchor || !explanation || !canonicalName || !visible) return null;\n    if (!containsNormalizedPhrase(canonicalName, anchor) && normalizeName(canonicalName) !== normalizeName(anchor)) return null;\n    if (!containsNormalizedPhrase(visible, anchor)) return null;\n    if (!verifiedCurrentVisibleExcerpts(record, visible)) return null;\n    return { anchor, explanation };\n}`;
  const newIdentity = `function identityEvidenceVerified(patch, policy, currentAdmissionText = '') {\n    const record = identityEvidenceRecord(patch);\n    if (!record) return null;\n    const anchor = humanIdentityCandidate(record.anchor, patch?.role);\n    const explanation = String(record.explanation || '').trim();\n    const canonicalName = canonicalPatchName(patch, []);\n    const visible = currentVisibleEvidenceText(policy, currentAdmissionText);\n    if (!anchor || !explanation || !canonicalName || !visible) return null;\n    if (!containsNormalizedPhrase(visible, anchor)) return null;\n    if (!verifiedCurrentVisibleExcerpts(record, visible)) return null;\n    const excerpts = Array.isArray(record?.excerpts)\n        ? record.excerpts.map(value => String(value || '').trim()).filter(Boolean)\n        : [];\n    if (!excerpts.some(excerpt => containsNormalizedPhrase(excerpt, anchor))) return null;\n    return { anchor, explanation };\n}`;
  s = replaceOnce(s, oldIdentity, newIdentity, 'identityEvidenceVerified');

  const oldUnique = `function identityAnchorUnique(state, patch, anchor, patches = []) {\n    const key = normalizeName(anchor);\n    if (!key) return false;\n    const owners = new Set();\n    for (const npc of state?.npcs || []) {\n        for (const label of [npc?.name, ...(Array.isArray(npc?.aliases) ? npc.aliases : [])]) {\n            if (containsNormalizedPhrase(label, anchor)) owners.add('npc:' + npc.id);\n        }\n    }\n    for (const candidate of Array.isArray(patches) ? patches : []) {\n        const name = canonicalPatchName(candidate, []);\n        if (!name || !containsNormalizedPhrase(name, anchor)) continue;\n        owners.add('patch:' + normalizeName(name));\n    }\n    const target = 'patch:' + normalizeName(canonicalPatchName(patch, []));\n    return owners.size === 1 && owners.has(target);\n}`;
  const newUnique = `function identityAnchorUnique(state, patch, anchor, patches = []) {\n    const key = normalizeName(anchor);\n    if (!key) return false;\n    const owners = new Set();\n    for (const npc of state?.npcs || []) {\n        for (const label of [npc?.name, ...(Array.isArray(npc?.aliases) ? npc.aliases : [])]) {\n            if (containsNormalizedPhrase(label, anchor)) owners.add('npc:' + npc.id);\n        }\n    }\n    for (let index = 0; index < (Array.isArray(patches) ? patches.length : 0); index += 1) {\n        const candidate = patches[index];\n        const name = canonicalPatchName(candidate, []);\n        const claimedAnchor = humanIdentityCandidate(identityEvidenceRecord(candidate)?.anchor, candidate?.role);\n        if (!name) continue;\n        if (containsNormalizedPhrase(name, anchor) || normalizeName(claimedAnchor) === key) owners.add('patch:' + index);\n    }\n    const targetIndex = (Array.isArray(patches) ? patches : []).indexOf(patch);\n    if (targetIndex < 0) return false;\n    const target = 'patch:' + targetIndex;\n    return owners.size === 1 && owners.has(target);\n}`;
  s = replaceOnce(s, oldUnique, newUnique, 'identityAnchorUnique');

  const start = s.indexOf('function verifiedIdentityAnchorIntroducesPatch');
  const end = s.indexOf('function newPatchAllowedByEvidence', start);
  if (start < 0 || end < 0) throw new Error('identity bridge block not found');
  const replacement = `function worldStateCanonicalIdentityMention(patch, policy) {\n    if (!policy?.detected) return false;\n    const canonicalName = canonicalPatchName(patch, []);\n    if (!canonicalName || looksLikeRoleLabel(canonicalName, patch?.role)) return false;\n    return containsNormalizedPhrase(policy.worldPresentText || '', canonicalName)\n        || containsNormalizedPhrase(policy.worldOffscreenText || '', canonicalName);\n}\nfunction verifiedIdentityAnchorIntroducesPatch(state, patch, policy, currentAdmissionText = '', patches = []) {\n    const identity = identityEvidenceVerified(patch, policy, currentAdmissionText);\n    if (!identity || !identityAnchorUnique(state, patch, identity.anchor, patches)) return false;\n    const canonicalName = canonicalPatchName(patch, []);\n    const anchorNamesCanonical = containsNormalizedPhrase(canonicalName, identity.anchor)\n        || normalizeName(canonicalName) === normalizeName(identity.anchor);\n    return anchorNamesCanonical || worldStateCanonicalIdentityMention(patch, policy);\n}\n\n`;
  s = s.slice(0, start) + replacement + s.slice(end);

  const oldAllowed = `    const scope = restrictedEvidenceScope(state, patch, policy);\n    if (scope === 'inner' || scope === 'excluded') return false;\n    if (scope === 'world') return worldStateIdentityBridgesVisibleIntroduction(state, patch, policy, visible, patches);\n    return false;`;
  s = replaceOnce(s, oldAllowed, '    return false;', 'newPatchAllowedByEvidence fallback');

  s = replaceOnce(s,
    `    if (identityEvidenceVerified(patch, policy, currentAdmissionText)) return true;\n    return visibleRoleIntroductionForPatch(patch, visible);`,
    `    if (verifiedIdentityAnchorIntroducesPatch(state, patch, policy, currentAdmissionText, patches)) return true;\n    return false;`,
    'newReferenceAllowedByWorldIdentityBridge');

  for (const retired of ['visibleRoleIntroductionForPatch', 'roleIdentityCues', 'WORLD_IDENTITY_GENERIC_ROLE_HEADS', 'WORLD_IDENTITY_INTRO_WORDS', 'worldStateIdentityBridgesVisibleIntroduction', 'restrictedEvidenceScope', 'evidenceReferenceScope']) {
    if (s.includes(retired)) throw new Error(`retired identity authority still present: ${retired}`);
  }
  write(p, s);
}

// Focused v0.5.28 production-path regressions.
write('tests/v0528-world-identity-enrichment.test.mjs', `import test from 'node:test';\nimport assert from 'node:assert/strict';\nimport fs from 'node:fs';\n\nimport { applyScanResult } from '../src/scanner.js';\nimport { createEmptyState } from '../src/schema.js';\nimport { identityPresencePromptRules, structuredEvidencePromptRules } from '../src/evidence-adapter.js';\n\nconst ZERO = { evaluated: true, impact: 'none', delta: { trust: 0, affection: 0, desire: 0, tension: 0 }, priority: [], axisEvidence: {}, evidence: '', reason: 'Routine first contact.' };\n\nfunction namedPatch({ name = 'Maren Keller', anchor = 'clerk', identityExcerpt, activityExcerpt = identityExcerpt, role = 'Guild intake clerk' } = {}) {\n  return {\n    id: '', name, identityKind: 'named', role,\n    identityEvidence: { anchor, excerpts: [identityExcerpt], explanation: 'The current visible individual is the canonically identified NPC.' },\n    activityEvidence: {\n      exchangeActive: { excerpts: [activityExcerpt], explanation: 'The NPC acts in the current exchange.' },\n      inChat: { excerpts: [activityExcerpt], explanation: 'The NPC remains relevant at scene end.' },\n    },\n    relationshipChange: structuredClone(ZERO),\n    relationshipSummary: 'Professional first-contact intake interaction.',\n    relationshipSummaryEvidence: { excerpts: [activityExcerpt], explanation: 'The NPC handles the player intake.' },\n  };\n}\n\nfunction payload(patches, refs = patches.map(row => row.name)) {\n  return { exchangeActiveNpcIds: refs, inChatNpcIds: refs, worldActiveNpcIds: [], npcs: patches, socialEdges: [], familyFacts: [], lifeStateUpdates: [], candidateAccounting: {} };\n}\n\nfunction policy(visible, { present = '', offscreen = '', inner = '', excluded = '' } = {}) {\n  return {\n    detected: true, visibleText: visible,\n    worldStateText: [present, offscreen].filter(Boolean).join('\\n'),\n    worldPresentText: present, worldOffscreenText: offscreen, worldOtherText: '',\n    innerChatterText: inner, excludedText: excluded, excludedTags: [], relationshipSources: [{ id: 'assistant-visible', kind: 'visible', role: 'assistant', text: visible }],\n  };\n}\n\nfunction apply(result, visible, evidencePolicy, admissionMode = 'balanced') {\n  const state = createEmptyState('chat:v0528-world-identity');\n  state.branchSafety = { status: 'safe' };\n  return applyScanResult(state, result, {\n    sourceMessageId: 2, turn: 1, currentAdmissionText: visible, profileContext: visible, semanticEvidenceContext: visible, relationshipContext: visible,\n    evidencePolicy, admissionMode, applyRelationship: false, preservePresence: true, preserveObservation: true,\n  });\n}\n\ntest('visible unique role anchor can enrich to one canonical World_State name without role-word heuristics', () => {\n  const visible = 'The clerk came out from behind the counter and steered you toward the reception planking.';\n  const patch = namedPatch({ identityExcerpt: visible });\n  const result = apply(payload([patch]), visible, policy(visible, { present: 'Maren Keller: Guild Intake Clerk' }));\n  const npc = result.state.npcs.find(row => row.name === 'Maren Keller');\n  assert.ok(npc);\n  assert.equal(result.patchResolutions[0].status, 'accepted');\n  assert.deepEqual(result.exchangeActiveNpcIds, [npc.id]);\n});\n\ntest('structured-only canonical name is never accepted as identityEvidence.anchor', () => {\n  const visible = 'The clerk came out from behind the counter.';\n  const patch = namedPatch({ anchor: 'Maren Keller', identityExcerpt: visible });\n  const result = apply(payload([patch]), visible, policy(visible, { present: 'Maren Keller: Guild Intake Clerk' }));\n  assert.equal(result.state.npcs.length, 0);\n  assert.equal(result.patchResolutions[0].reason, 'identity-evidence-unresolved');\n});\n\ntest('visible role anchor cannot invent a proper name without current World_State canonical corroboration', () => {\n  const visible = 'The clerk came out from behind the counter.';\n  const patch = namedPatch({ identityExcerpt: visible });\n  const result = apply(payload([patch]), visible, policy(visible));\n  assert.equal(result.state.npcs.length, 0);\n  assert.equal(result.patchResolutions[0].reason, 'identity-evidence-unresolved');\n});\n\ntest('World_State canonical corroboration does not rescue fabricated or anchor-disconnected identity excerpts', () => {\n  const visible = 'The clerk came out from behind the counter. A ledger lies beside the wax seal.';\n  const fabricated = namedPatch({ identityExcerpt: 'The clerk stamped the ledger.' });\n  const disconnected = namedPatch({ identityExcerpt: 'A ledger lies beside the wax seal.' });\n  for (const patch of [fabricated, disconnected]) {\n    const result = apply(payload([patch]), visible, policy(visible, { present: 'Maren Keller: Guild Intake Clerk' }));\n    assert.equal(result.state.npcs.length, 0);\n    assert.equal(result.patchResolutions[0].reason, 'identity-evidence-unresolved');\n  }\n});\n\ntest('shared visible role anchors fail closed across competing named proposals', () => {\n  const a = 'The clerk in blue opens one ledger.';\n  const b = 'The clerk in red opens another ledger.';\n  const visible = a + ' ' + b;\n  const first = namedPatch({ name: 'Maren Keller', identityExcerpt: a, activityExcerpt: a });\n  const second = namedPatch({ name: 'Tessa Vale', identityExcerpt: b, activityExcerpt: b });\n  const present = 'Maren Keller: Guild Intake Clerk\\nTessa Vale: Guild Intake Clerk';\n  const result = apply(payload([first, second]), visible, policy(visible, { present }));\n  assert.equal(result.state.npcs.length, 0);\n  assert.deepEqual(result.patchResolutions.map(row => row.status), ['unresolved', 'unresolved']);\n});\n\ntest('unique visible short proper name still enriches to the compatible World_State full name', () => {\n  const visible = 'Maren points to the ledger and waits.';\n  const patch = namedPatch({ anchor: 'Maren', identityExcerpt: visible });\n  const result = apply(payload([patch]), visible, policy(visible, { present: 'Maren Keller: Guild Intake Clerk' }));\n  assert.ok(result.state.npcs.some(row => row.name === 'Maren Keller'));\n});\n\ntest('named-preferred accepts valid named enrichment while manual admission still blocks automatic creation', () => {\n  const visible = 'The clerk points to the ledger and waits.';\n  const patch = namedPatch({ identityExcerpt: visible });\n  const p = policy(visible, { present: 'Maren Keller: Guild Intake Clerk' });\n  assert.equal(apply(payload([patch]), visible, p, 'named_preferred').state.npcs.length, 1);\n  assert.equal(apply(payload([patch]), visible, p, 'manual').state.npcs.length, 0);\n});\n\ntest('canonical name in private or excluded structured material cannot enrich a visible anchor', () => {\n  const visible = 'The clerk points to the ledger and waits.';\n  const patch = namedPatch({ identityExcerpt: visible });\n  for (const p of [policy(visible, { inner: 'Maren Keller' }), policy(visible, { excluded: 'Maren Keller' })]) {\n    const result = apply(payload([patch]), visible, p);\n    assert.equal(result.state.npcs.length, 0);\n  }\n});\n\ntest('identity contract tells the model to keep visible anchor wording during World_State name enrichment', () => {\n  const identity = identityPresencePromptRules().join(' ');\n  const structured = structuredEvidencePromptRules().join(' ');\n  assert.match(identity, /proper\\/short name or unique role\\/description/);\n  assert.match(structured, /Keep identityEvidence\\.anchor as the visible wording/);\n  assert.match(structured, /structured-only canonical name belongs in name, never anchor/);\n});\n\ntest('superseded deterministic role-head admission authority is removed', () => {\n  const source = fs.readFileSync(new URL('../src/scan-application.js', import.meta.url), 'utf8');\n  for (const retired of ['visibleRoleIntroductionForPatch', 'roleIdentityCues', 'WORLD_IDENTITY_GENERIC_ROLE_HEADS', 'WORLD_IDENTITY_INTRO_WORDS', 'worldStateIdentityBridgesVisibleIntroduction']) {\n    assert.doesNotMatch(source, new RegExp(retired));\n  }\n});\n`);

// Release labels and concise maintenance docs.
{
  const manifest = JSON.parse(read('manifest.json'));
  manifest.version = '0.5.28';
  write('manifest.json', JSON.stringify(manifest, null, 4) + '\n');
  let schema = read('src/schema.js').replace("NPC_STATE_VERSION = '0.5.27'", "NPC_STATE_VERSION = '0.5.28'");
  write('src/schema.js', schema);
  let structure = read('tests/structure.test.mjs').replaceAll('0.5.27', '0.5.28');
  write('tests/structure.test.mjs', structure);
  let dev = read('DEVELOPMENT.md').replace('- Extension release: `0.5.27`', '- Extension release: `0.5.28`');
  write('DEVELOPMENT.md', dev);

  let core = read('docs/core-contract.md');
  const old = 'New NPCs use an empty `id`, canonical `name`, and `identityKind` `named` or `role-label` with current-visible identity/activity evidence; the extension assigns stored IDs locally. When a canonical contextual name or role label is richer than the literal visible wording, a shorter `identityEvidence.anchor` may satisfy Balanced-mode admission only after its bounded identity excerpts validate exactly against current-visible evidence, the anchor itself occurs inside one of those validated excerpts, and the anchor is uniquely owned by that proposed NPC in the operation. This does not bypass Named preferred or Manual admission policy, identity-collision checks, or the structured/private evidence firewall.';
  const neu = 'New NPCs use an empty `id`, canonical `name`, and `identityKind` `named` or `role-label` with current-visible identity/activity evidence; the extension assigns stored IDs locally. `identityEvidence.anchor` is the actual current-visible proper/short name or unique role/description for the individual. Its bounded excerpts must validate exactly against current-visible evidence, include that anchor, and leave the anchor uniquely owned in the operation. A richer role label may be admitted directly when the anchor identifies that canonical label; a proper canonical name that is not visible may be enriched only when that same current World_State placement names the already grounded individual. The structured-only name never becomes the anchor and World_State alone never introduces an NPC. This does not bypass Named preferred or Manual admission policy, identity-collision checks, or the structured/private evidence firewall.';
  core = replaceOnce(core, old, neu, 'core identity paragraph');
  write('docs/core-contract.md', core);

  let readme = read('README.md');
  readme = readme.replace('## Release 0.5.27\n\n0.5.27 fixes intermittent first-seen NPC creation when the scanner returns a contextual role label whose canonical dossier name is richer than the literal visible anchor. A current-visible, exact-validated, uniquely owned `identityEvidence.anchor` can now satisfy new-NPC admission in Balanced mode; fabricated excerpts, anchors absent from their own identity evidence, ambiguous shared anchors, structured-only introductions, and Named preferred/Manual policy bypasses remain rejected.',
    '## Release 0.5.28\n\n0.5.28 unifies first-seen identity grounding: a visible proper/short name or unique role/description is the sole `identityEvidence.anchor`, and current World_State may enrich that already grounded individual with one compatible canonical proper name. The superseded deterministic role-head fallback is removed; structured-only names, fabricated/disconnected excerpts, ambiguous anchors, and policy/collision bypasses remain rejected.');
  readme = readme.replace('- **0.5.27:** admit first-seen contextual role labels through their exact validated unique identity anchor in Balanced mode, without weakening identity collisions, structured evidence firewalls, or stricter admission settings.', '- **0.5.27:** admit first-seen contextual role labels through their exact validated unique identity anchor in Balanced mode, without weakening identity collisions, structured evidence firewalls, or stricter admission settings.\n- **0.5.28:** unify visible identity anchors with optional current-World_State canonical-name enrichment and retire the older deterministic role-head bridge.');
  write('README.md', readme);

  let changelog = read('CHANGELOG.md');
  const entry = `## 0.5.28\n\n- Unify first-seen identity admission around one validated current-visible identity anchor. A proper/short name or unique role/description must appear inside exact current-visible identity evidence; a compatible proper canonical name may be enriched from the same current World_State placement without turning that structured-only name into the anchor.\n- Remove the superseded deterministic role-head/intro-word World_State fallback instead of keeping two competing identity authorities. Preserve identity collision checks, unique-anchor ownership, structured/private firewalls, Balanced/Named preferred/Manual admission semantics, and existing activity/relationship separation.\n- Add Maren-style production regressions for role-to-canonical enrichment, structured-only-anchor rejection, fabricated/disconnected evidence, ambiguous shared roles, short-name enrichment, admission policies, private/excluded rejection, prompt consistency, and retired-code absence. Persisted schema remains version 1.\n\n`;
  changelog = changelog.replace('## 0.5.27\n', entry + '## 0.5.27\n');
  write('CHANGELOG.md', changelog);
}

// Temporary baseline helper must never ship.
if (fs.existsSync('tests/release-prompt-measure.tmp.test.mjs')) fs.rmSync('tests/release-prompt-measure.tmp.test.mjs');
console.log('Applied v0.5.28 release candidate patch.');
