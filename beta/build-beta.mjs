import fs from 'node:fs';

const VERSION = '0.4.0-beta.1';
const read = path => fs.readFileSync(path, 'utf8');
const write = (path, value) => fs.writeFileSync(path, value);
function replaceOnce(path, from, to) {
    const source = read(path);
    if (!source.includes(from)) throw new Error(`Expected marker not found in ${path}: ${from.slice(0, 100)}`);
    write(path, source.replace(from, to));
}

// Manifest/runtime identity. Beta intentionally uses an isolated SillyTavern settings key.
const manifest = JSON.parse(read('manifest.json'));
manifest.display_name = 'NPC State Beta';
manifest.version = VERSION;
manifest.loading_order = 111;
write('manifest.json', JSON.stringify(manifest, null, 4) + '\n');

replaceOnce('v03/schema.js', "export const NPC_STATE_VERSION = '0.3.2';", `export const NPC_STATE_VERSION = '${VERSION}';`);
replaceOnce('v03/index.js', "const EXTENSION_NAME = 'npc_state';", "const EXTENSION_NAME = 'npc_state_beta';");
replaceOnce('v03/index.js', "const PROMPT_KEY = 'npc_state_v3_live_dossier';", "const PROMPT_KEY = 'npc_state_v04_beta_foreground';");
replaceOnce('v03/index.js', "import { buildInjection } from './injection.js';", "import { buildInjection } from './injection.js';\nimport { consumeNpcStateControl } from './foreground.js';");

// Never let stable and beta installations write the same sidecar or localStorage locks.
let storage = read('v03/storage.js');
storage = storage
    .replace("export const V3_FILE_FORMAT = 'npc_state_v3_chat_data';", "export const V3_FILE_FORMAT = 'npc_state_v04_beta_chat_data';")
    .replaceAll('npc-state-v3-', 'npc-state-v04-beta-')
    .replaceAll('npc-state-v3-sidecar:', 'npc-state-v04-beta-sidecar:')
    .replaceAll('NPC_STATE_V3_', 'NPC_STATE_V04_BETA_');
write('v03/storage.js', storage);

// Embedded contract calls this inChat; internally v0.3 schema keeps the proven `present` field.
let scanner = read('v03/scanner.js');
scanner = scanner.replace(
    'finalPresentNpcIds: uniqueStrings(parsed.finalPresentNpcIds),',
    'finalPresentNpcIds: uniqueStrings(parsed.inChatNpcIds ?? parsed.finalPresentNpcIds),',
);
write('v03/scanner.js', scanner);

// Apply an already-produced foreground observation without launching generateRaw.
const engineMarker = '    async function refreshDossier(reference) {';
const embeddedApply = `    async function applyEmbeddedScan(messageId, parsed) {\n        const chatKey = getChatKey();\n        if (!chatKey || chatKey === 'no-chat' || /-pending:/.test(chatKey)) return { ok: false, reason: 'no-chat' };\n        const settings = getSettings();\n        if (settings.enabled === false || settings.autoScan === false) return { ok: false, reason: 'auto-disabled' };\n        return exclusive(chatKey, async () => {\n            const state = await loadChat(chatKey);\n            if (!state) return { ok: false, reason: 'no-state' };\n            if (state.branchSafety?.status !== 'safe') return { ok: false, reason: 'branch-unsafe', messageId };\n            const ctx = getContext();\n            const chat = ctx.chat || [];\n            const message = chat[messageId];\n            if (!message || message.is_system || message.is_user) return { ok: false, reason: 'not-assistant-message' };\n            const working = normalizeState(state, chatKey);\n            working.turn = Math.max(0, Number(working.turn) || 0) + 1;\n            const applied = applyScanResult(working, parsed, {\n                sourceMessageId: messageId,\n                turn: working.turn,\n                relationshipCaps: settings.relationshipCaps || DEFAULT_RELATIONSHIP_CAPS,\n                dossierLimits: settings.dossierLimits,\n            });\n            const relationshipHistoryLimit = normalizeRelationshipHistoryLimit(settings.relationshipHistoryLimit);\n            applied.state = trimStateRelationshipHistory(applied.state, relationshipHistoryLimit);\n            const exchange = currentExchange(chat, messageId) || { assistant: { ...message, id: messageId }, user: null };\n            const referencedNpcIds = referencedNpcIdsFromExchange(applied.state, exchange);\n            const stale = applyStaleLifecycle(applied.state, {\n                settings,\n                currentTurn: narrativeTurnForMessage(chat, messageId),\n                sourceMessageId: messageId,\n                exchangeActiveNpcIds: applied.exchangeActiveNpcIds,\n                finalPresentNpcIds: applied.finalPresentNpcIds,\n                worldActiveNpcIds: applied.worldActiveNpcIds,\n                referencedNpcIds,\n            });\n            let committed = recordCheckpoint(stale.state, chat, messageId, 'embedded-foreground');\n            committed.lastScannedMessageId = messageId;\n            committed.updatedAt = Date.now();\n            const persisted = await persist(chatKey, committed);\n            const notice = lifecycleNotice(stale);\n            if (notice) notify('info', \\`Stale management \\${notice}.\\`);\n            return {\n                ok: true,\n                messageId,\n                embedded: true,\n                exchangeActiveNpcIds: applied.exchangeActiveNpcIds,\n                finalPresentNpcIds: applied.finalPresentNpcIds,\n                worldActiveNpcIds: applied.worldActiveNpcIds,\n                referencedNpcIds,\n                targetNpcIds: applied.targetNpcIds,\n                state: structuredClone(persisted),\n            };\n        });\n    }\n\n`;
let engine = read('v03/engine.js');
if (!engine.includes(engineMarker)) throw new Error('Engine refresh marker missing');
engine = engine.replace(engineMarker, embeddedApply + engineMarker);
engine = engine.replace('        scan,\n        refreshDossier,', '        scan,\n        applyEmbeddedScan,\n        refreshDossier,');
write('v03/engine.js', engine);

// Build the foreground prompt from canonical state. The model does a full semantic scan,
// but the extension remains authoritative for identity merge, relationship caps, history,
// stale lifecycle and persistence.
write('v03/injection.js', `function line(label, value) {\n    const text = String(value ?? '').trim();\n    return text ? \\`\\${label}: \\${text}\\` : '';\n}\n\nfunction fullNpc(npc) {\n    const rel = npc.relationship || {};\n    return [\n        \\`NPC \\${npc.id} | \\${npc.name}\\${npc.role ? \\` | \\${npc.role}\\` : ''}\\`,\n        line('Aliases', (npc.aliases || []).join(' | ')),\n        line('Species', npc.species), line('Age', npc.age), line('Apparent age', npc.apparentAge),\n        line('Appearance', npc.appearance), line('Personality', npc.personality),\n        line('Behavior', (npc.behaviorProfile || []).join(' | ')), line('Speech', npc.speech),\n        line('Mannerisms', (npc.mannerisms || []).join(' | ')), line('Background', npc.background),\n        line('Mood', npc.mood), line('Location', npc.location), line('Goal', npc.goal), line('Status', npc.status),\n        \\`Relationship toward PLAYER: trust \\${Number(rel.trust) || 0}, affection \\${Number(rel.affection) || 0}, desire \\${Number(rel.desire) || 0}, tension \\${Number(rel.tension) || 0}\\`,\n        line('Relationship summary', npc.relationshipSummary),\n        line('Key non-player relationships', (npc.keyRelationships || []).join(' | ')),\n        line('Important memories', (npc.memories || []).join(' | ')),\n    ].filter(Boolean).join('\\n');\n}\n\nfunction identityDirectory(state) {\n    return (state?.npcs || []).slice(0, 400).map(npc =>\n        [npc.id, npc.name, (npc.aliases || []).join('/'), npc.role, npc.archived ? 'archived' : 'active'].join(' | ')\n    ).join('\\n');\n}\n\nfunction activeCandidates(state, limit) {\n    const activeIds = new Set([\n        ...(state?.lastObservation?.exchangeActiveNpcIds || []),\n        ...(state?.lastObservation?.finalPresentNpcIds || []),\n        ...(state?.lastObservation?.worldActiveNpcIds || []),\n    ]);\n    return (state?.npcs || []).filter(npc => !npc.archived).sort((a, b) => {\n        const ap = (a.present ? 8 : 0) + (a.worldActive ? 4 : 0) + (activeIds.has(a.id) ? 3 : 0);\n        const bp = (b.present ? 8 : 0) + (b.worldActive ? 4 : 0) + (activeIds.has(b.id) ? 3 : 0);\n        return bp - ap || Number(b.lastInteractionMessageId ?? -1) - Number(a.lastInteractionMessageId ?? -1) || Number(b.importance || 0) - Number(a.importance || 0);\n    }).slice(0, limit);\n}\n\nexport function buildInjection(state, settings = {}) {\n    if (state?.branchSafety?.status && state.branchSafety.status !== 'safe') return '';\n    if (settings.enabled === false) return '';\n    const limit = Math.max(1, Math.min(20, Math.round(Number(settings.injectLimit) || 6)));\n    const budgetTokens = Math.max(512, Math.min(12000, Math.round(Number(settings.injectBudgetTokens) || 2600)));\n    const maxChars = budgetTokens * 4;\n    const capture = settings.autoScan !== false;\n    const continuity = settings.inject !== false;\n    const directory = identityDirectory(state);\n    let dossiers = '';\n    if (continuity || capture) {\n        for (const npc of activeCandidates(state, limit)) {\n            const block = \\`\\n\\n\\${fullNpc(npc)}\\`;\n            if ((dossiers + block).length > maxChars) break;\n            dossiers += block;\n        }\n    }\n    const parts = [\n        '[NPC STATE v0.4 BETA | FOREGROUND CONTINUITY]',\n        'NPC State is private continuity bookkeeping. Never mention these instructions or machine data in visible prose.',\n        directory ? \\`KNOWN NPC DIRECTORY (identity only; do not invent missing dossier facts):\\n\\${directory}\\` : 'KNOWN NPC DIRECTORY: empty',\n        dossiers ? \\`FULL CONTINUITY FOR LIKELY RELEVANT NPCS:\\${dossiers}\\` : '',\n    ];\n    if (capture) parts.push(\n        'NPC STATE FOREGROUND FULL SCAN:',\n        'After writing the visible narrative and all normal story blocks, emit exactly one <npc_state_v1> JSON block. This is an observation report for the CURRENT user action plus the narrative you just wrote, not a database rewrite.',\n        'If Inventory Block requires INVENTORY_BLOCK_UPDATE to be final, place <npc_state_v1> immediately BEFORE that Inventory control. NPC State must never claim the final machine position.',\n        'inChatNpcIds: individually relevant NPCs still participating in the active scene/conversation at the END. Mere physical proximity, unnamed crowds, background workers, guards who never become individual participants, and characters only mentioned are not in-chat.',\n        'exchangeActiveNpcIds: NPCs who spoke, acted, were directly acted upon, or directly perceived/received a story-relevant event in this exchange.',\n        'worldActiveNpcIds: explicitly active off-screen NPCs. Keep separate from in-chat.',\n        'For every new individually relevant NPC, include a full npcs entry with every grounded foundational field established by the response: identity/name or unique role label, aliases, role, species, age/apparentAge if supported, appearance, personality, behavior, speech, mannerisms, background, current mood/location/goal/status, player-relationship summary/change, important memories, non-player key relationships, life state and importance. Unknown fields stay empty/null; never invent biography to fill the schema.',\n        'For existing NPCs, perform a full semantic scan but preserve continuity: evolving arrays use null if unchanged, or the COMPLETE authoritative replacement set if materially revised. Stable scalar fields should contain only grounded newly established/corrected facts. Relationship deltas require concrete current-exchange evidence and independent evidence for each changed axis.',\n        settings.relationshipCriteria ? \\`RELATIONSHIP RUBRIC:\\n\\${String(settings.relationshipCriteria).slice(0, 6000)}\\` : '',\n        settings.memoryCriteria ? \\`IMPORTANT MEMORY RUBRIC:\\n\\${String(settings.memoryCriteria).slice(0, 6000)}\\` : '',\n        'The PLAYER/current user persona is never an NPC. keyRelationships/socialEdges are NPC-to-NPC only.',\n        'OUTPUT SHAPE (JSON inside <npc_state_v1>): {"exchangeActiveNpcIds":[],"inChatNpcIds":[],"worldActiveNpcIds":[],"npcs":[{"id":"existing id or empty","name":"canonical name or unique role label","aliases":[],"role":"","species":"","age":"","apparentAge":"~N or empty","appearance":"","personality":"","behaviorProfile":null,"speech":"","mannerisms":null,"background":"","keyRelationships":null,"memories":null,"relationshipSummary":"","mood":"","location":"","goal":"","status":"","importance":0,"lifeState":"alive|dead|unknown","lifeStateCertainty":"explicit|strong|uncertain","lifeStateReason":"","livingReturn":false,"relationshipChange":{"impact":"none|ordinary|meaningful|major|extreme","delta":{"trust":0,"affection":0,"desire":0,"tension":0},"evidence":"","reason":""}}],"socialEdges":[]}',\n        'Emit the machine block even when no NPC changed, because an empty inChatNpcIds is meaningful. Do not put markdown fences around it.'\n    );\n    return parts.filter(Boolean).join('\\n\\n');\n}\n`);

write('v03/foreground.js', `import { parseScanJson } from './scanner.js';\n\nconst OPEN = /<npc_state_v1\\b[^>]*>/i;\nconst CLOSE = /<\\/npc_state_v1\\s*>/i;\n\nexport function consumeNpcStateControl(messageText) {\n    const source = String(messageText ?? '');\n    const open = OPEN.exec(source);\n    if (!open) return { found: false, cleanedText: source, parsed: null, raw: '', errors: [] };\n    const bodyStart = open.index + open[0].length;\n    const closeMatch = CLOSE.exec(source.slice(bodyStart));\n    if (!closeMatch) {\n        return { found: true, cleanedText: source.slice(0, open.index).trimEnd(), parsed: null, raw: source.slice(open.index), errors: ['NPC State foreground block was truncated or missing its closing tag.'] };\n    }\n    const closeStart = bodyStart + closeMatch.index;\n    const end = closeStart + closeMatch[0].length;\n    const raw = source.slice(open.index, end);\n    const body = source.slice(bodyStart, closeStart).trim();\n    const duplicate = OPEN.test(source.slice(end));\n    const cleanedText = (source.slice(0, open.index) + source.slice(end)).replace(/\\n{3,}/g, '\\n\\n').trimEnd();\n    const errors = [];\n    if (duplicate) errors.push('Multiple NPC State foreground blocks were emitted; update rejected.');\n    let parsed = null;\n    if (!errors.length) {\n        try { parsed = parseScanJson(body); }\n        catch (error) { errors.push(error instanceof Error ? error.message : String(error)); }\n    }\n    return { found: true, cleanedText, parsed, raw, errors };\n}\n`);

// Replace automatic post-response generateRaw with consume/validate/apply of the embedded block.
let index = read('v03/index.js');
const oldAuto = `async function runAutomaticScan(messageId) {\n    try {\n        const result = await engine.scan(messageId, { manual: false });\n        if (result?.ok || result?.discarded) refreshSurfaces();\n    } catch (error) {\n        console.error('[NPC State v0.3] automatic scan failed safely', error);\n        notify('error', \\`automatic scan failed without committing partial state. \\${error?.message || error}\\`);\n    }\n}`;
const newAuto = `async function processEmbeddedScan(messageId) {\n    const ctx = getContext();\n    const id = Number(messageId);\n    const message = ctx?.chat?.[id];\n    if (!Number.isInteger(id) || !message || message.is_user || message.is_system) return { ok: false, reason: 'not-assistant-message' };\n    const consumed = consumeNpcStateControl(message.mes);\n    if (!consumed.found) {\n        console.warn('[NPC State Beta] Foreground response omitted <npc_state_v1>; state left unchanged. Use Scan current cast for manual recovery if needed.');\n        return { ok: false, reason: 'missing-control' };\n    }\n    // Strip only from the current live message. This composes safely with Inventory Block\n    // regardless of which extension consumes its own machine transport first.\n    message.mes = consumed.cleanedText;\n    message.extra ??= {};\n    message.extra.npc_state_beta_v1 = { version: 1, accepted: consumed.errors.length === 0, payload: consumed.errors.length ? null : consumed.raw, at: Date.now() };\n    const swipeId = Number.isInteger(message.swipe_id) ? message.swipe_id : 0;\n    const swipe = Array.isArray(message.swipe_info) ? message.swipe_info[swipeId] : null;\n    if (swipe) { swipe.extra ??= {}; swipe.extra.npc_state_beta_v1 = structuredClone(message.extra.npc_state_beta_v1); }\n    setTimeout(() => { try { ctx.updateMessageBlock?.(id, ctx.chat?.[id]); } catch {} }, 0);\n    try { const save = ctx.saveChat?.(); if (save?.catch) save.catch(() => {}); } catch {}\n    if (consumed.errors.length || !consumed.parsed) {\n        console.warn('[NPC State Beta] Foreground NPC payload rejected.', consumed.errors);\n        notify('warning', 'embedded NPC scan was malformed and was discarded. Use Scan current cast for recovery.');\n        return { ok: false, reason: 'invalid-control', errors: consumed.errors };\n    }\n    try {\n        const result = await engine.applyEmbeddedScan(id, consumed.parsed);\n        if (result?.ok) refreshSurfaces();\n        return result;\n    } catch (error) {\n        console.error('[NPC State Beta] embedded scan failed safely', error);\n        notify('error', \\`embedded scan failed without committing partial state. \\${error?.message || error}\\`);\n        return { ok: false, reason: 'apply-failed', error };\n    }\n}`;
if (!index.includes(oldAuto)) throw new Error('Automatic scan function marker missing');
index = index.replace(oldAuto, newAuto);
index = index.replace('void runAutomaticScan(messageId);', 'void processEmbeddedScan(messageId);');
index = index.replace('await engine.reconcileBranch({ rescan: true });', 'await engine.reconcileBranch({ rescan: false });');
index = index.replaceAll('[NPC State v0.3]', '[NPC State Beta]');
write('v03/index.js', index);

// Beta UI wording: `present` storage now means individually relevant in-chat cast.
let ui = read('v03/ui.js');
ui = ui
    .replaceAll('v0.3.2', VERSION)
    .replace('Auto current-cast scan', 'Embedded current-cast scan')
    .replace('One batch scan after each assistant reply. No cast-wide backfill queue.', 'Uses the same foreground RP generation. Missing/malformed capture leaves state unchanged; Scan current cast is the manual repair tool.')
    .replace('Only strict final-scene physical presence is injected.', 'Injects the individually relevant in-chat cast, not incidental background bodies.')
    .replaceAll('PRESENT NPCS', 'IN-CHAT NPCS');
write('v03/ui.js', ui);

// Bootstrap/version comments only.
let bootstrap = read('bootstrap.js').replaceAll('v0.3.2', VERSION);
write('bootstrap.js', bootstrap);

write('README.md', `# NPC State Beta ${VERSION}\n\nExperimental one-pass foreground NPC continuity for SillyTavern.\n\n## What this beta changes\n\n- Normal automatic NPC accounting is performed in the **same LLM inference** that writes the RP response.\n- The model emits a hidden \\`<npc_state_v1>...<\\/npc_state_v1>\\` observation block after visible prose. NPC State validates/applies it locally and strips it from chat.\n- If Inventory Block 0.4 is installed, NPC State explicitly yields the terminal position: NPC payload first, then Inventory's \\`INVENTORY_BLOCK_UPDATE\\` control last.\n- Existing dossier schema, relationship caps/history, stale lifecycle, checkpoints and manual Scan current cast remain authoritative.\n- New NPCs are bootstrapped by the same full semantic foreground scan. Unknown biography is left empty rather than fabricated.\n- The old automatic second \\`generateRaw\\` scan is removed. Manual Scan current cast / dossier Refresh still use the proven separate scanner as recovery/reconciliation tools.\n- \\`present\\` storage is interpreted in this beta as **individually relevant NPCs in chat at the end of the exchange**, not every physically nearby background character.\n- Beta settings and sidecars are isolated from stable NPC State so both repositories cannot race the same files.\n\n## Beta caveat\n\nThis is deliberately a beta. If the foreground model omits or corrupts its machine block, no automatic dossier mutation is committed for that response. Use **Scan current cast** to repair that turn.\n`);

console.log(`Prepared NPC State ${VERSION}`);
